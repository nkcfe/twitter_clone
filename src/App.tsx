import Layout from "components/Layout";
import Router from "components/Router";
import React from "react";

const App = () => {
  return (
    <Layout>
      <Router />
    </Layout>
  );
};

export default App;
